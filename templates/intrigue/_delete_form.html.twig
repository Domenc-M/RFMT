<form method="post" action="{{ path('intrigue_delete', {'id': intrigue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intrigue.id) }}">
    <button class="btn">Delete</button>
</form>
