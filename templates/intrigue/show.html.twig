{% extends 'base.html.twig' %}

{% block title %}Intrigue{% endblock %}

{% block body %}
    <h1>Intrigue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intrigue.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ intrigue.name }}</td>
            </tr>
            <tr>
                <th>Summary</th>
                <td>{{ intrigue.summary }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ intrigue.type }}</td>
            </tr>
            <tr>
                <th>Fluff</th>
                <td>{{ intrigue.fluff }}</td>
            </tr>
            <tr>
                <th>Crunch</th>
                <td>{{ intrigue.crunch }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ intrigue.createdAt ? intrigue.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ intrigue.updatedAt ? intrigue.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_searchOwn') }}">back to list</a>

    <a href="{{ path('intrigue_edit', {'id': intrigue.id}) }}">edit</a>

    {{ include('intrigue/_delete_form.html.twig') }}
{% endblock %}
